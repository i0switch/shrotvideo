SNS動画自動生成ツール テストおよびデバッグ指示書
本ドキュメントは、SNS動画自動生成ツールの各開発フェーズにおけるテスト手順と、それに伴うデバッグ要件を定義するものです。

フェーズ1：現状のコードとロジックの把握
目的：
ソースコード全体と現在の処理ロジックを正確に理解し、後続のテストとデバッグの土台を築く。

指示：

リポジトリ内のすべてのソースコードを精査し、プログラムの全体構造を把握してください。

各機能、モジュール、外部ライブラリの依存関係をリストアップしてください。

「SNS投稿の取得」→「コンテンツの解析」→「動画生成」→「ファイル出力」という一連のデータフローと処理ロジックを分析してください。

上記1〜3の調査結果をanalysis_summary.txtとしてファイルに出力してください。

フェーズ2：内部テスト（開発環境での直接実行）
目的：
コアとなる動画生成ロジックが、指定された各SNSプラットフォームに対して正しく機能することを確認する。

指示：

以下の監視対象アカウントから、それぞれ最新3件の投稿を取得・処理してください。

TikTok: https://www.tiktok.com/@sonnawakenai.ai

YouTube Shorts: https://www.youtube.com/@BMYuya/shorts

X (旧Twitter): https://x.com/Mountain_cb

各投稿から3秒の動画クリップを生成してください。

成功条件：

以下の構成で、合計9個の動画ファイルが正常に出力されること。

TikTokアカウントからの動画 x 3

YouTube Shortsアカウントからの動画 x 3

Xアカウントからの動画 x 3

処理中に致命的なエラーが発生せず、エラーログが出力されないこと。

フェーズ3：GUIテスト（EXE化前）
目的：
GUIを介した操作がバックエンドのロジックと正しく連携し、意図通りに動作することを確認する。

指示：

（監視対象アカウントが登録済みであることを前提とする）

アプリケーションのGUIを起動してください。

GUI上にある「テスト生成」機能（またはそれに類するボタン）を実行してください。

成功条件：

フェーズ2と同様に、合計9個の動画ファイル（X, TikTok, YouTubeから各3個ずつ）が正常に出力されること。

フェーズ4：実行ファイル（.exe）テスト
目的：
ビルドされた実行ファイルが、エンドユーザー環境でスタンドアロンで完全に動作することを確認する。

指示：

（監視対象アカウントが登録済みであることを前提とする）

EXE化した実行ファイル（.exe）を起動してください。

GUI上にある「テスト生成」機能（またはそれに類するボタン）を実行してください。

成功条件：

フェーズ2、3と同様に、合計9個の動画ファイルが正常に出力されること。

共通デバッグ要件
指示：

テスト失敗時の自動対応： 上記のいずれかのテストフェーズで「成功条件」が満たされなかった場合、以下のデバッグサイクルをテストがパスするまで自動で繰り返してください。

ログ分析： error.logまたはコンソールに出力されたログ情報を即座に確認し、エラーの原因（例：APIレスポンスの変更、要素が見つからない、ファイル書き込み権限など）を特定する。

コード修正： 特定した原因に基づいて、ソースコードを自動的に修正する。

再テスト： 修正を適用した後、失敗したテストフェーズを再度実行し、成功条件を満たすか確認する。