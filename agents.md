エージェントAI向け開発指示書
この指示書は、自己認識と自律的なデバッグ能力を持つエージェントAIが、与えられた仕様に基づいて完全に機能するアプリケーションを開発するためのものです。

1. プロジェクト概要
このプロジェクトの目的は、YouTube、TikTokの指定されたアカウントを監視し、新しい動画がアップロードされた際に、そのコンテンツに基づいて自動的にオリジナル動画を生成するアプリケーションを開発することです。さらに、指定されたX(Twitter)アカウントがリツイートしたポストを自動的に動画に変換する機能も実装します。開発対象の実行環境はWindowsとMacの両方です。

2. 開発環境と既存コード
既存のコードベース: https://github.com/i0switch/shrotvideo/tree/new-feature

エージェントAIは、この既存のコードを完全に理解し、その上で開発を進める必要があります。

ビルドやテストに必要なコマンドは、既存のリポジトリ内のpackage.jsonやその他の設定ファイルから読み取る必要があります。

開発を開始する前に、すべての依存関係をインストールしてください。

3. 主要な開発タスク
ソース監視機能の実装: YouTube, TikTok, X(Twitter)のAPIまたはウェブスクレイピング技術を用いて、指定されたアカウントの新規コンテンツ（動画、リツイート）を正確に検知する機能を構築します。

動画生成エンジンの開発:

新規動画のアップロード検知後、そのコンテンツを元に新しいオリジナル動画を生成するアルゴリズムを開発します。

X(Twitter)のリツイートを画像として取得し、動画フォーマットに変換する機能を実装します。

生成される動画は、プラットフォームごとに適切なフォーマットと解像度を保持する必要があります。

設定管理機能の追加: 監視対象アカウント、生成動画の設定、X(Twitter)アカウントなど、ユーザーが設定を変更できるインターフェースを実装します。

4. テストとデバッグ
エージェントAIは、単にコードを生成するだけでなく、以下の手順に従って徹底的なデバッグとテストを自律的に実施する必要があります。

全コードチェック完了後、以下に示す作業をリストとして生成し、各項目を自律的に実行して完了するまで繰り返すこと。

単体テスト: 各機能（例：動画検知、動画生成）ごとに、個々のコンポーネントが仕様通りに動作するかを検証します。

統合テスト: 各機能が連携して動作するシナリオをテストします。

エンドツーエンドテスト: 監視から動画生成、保存までの一連のワークフロー全体をシミュレートし、完全な動作を保証します。

テストケースの網羅:

監視対象アカウントに新しい動画がアップロードされた場合。

監視対象アカウントに新しい動画がアップロードされない場合。

複数の新しい動画が同時にアップロードされた場合。

X(Twitter)アカウントが画像付きポストをリツイートした場合。

X(Twitter)アカウントがテキストのみのポストをリツイートした場合。

品質保証:

バグの自動修正: テスト中にバグが検出された場合、AIは自律的にコードを分析し、修正を適用します。

性能テスト: 大量のデータや高負荷な状況下でもアプリケーションが安定して動作するかを確認します。

5. 開発成果物と品質基準
最終成果物は、WindowsおよびMacで完全に正常に動作するアプリケーションです。

全ての機能が仕様通りに動作し、バグは存在しないものとします。

エージェントAIは、この指示書に従って開発が完了したことを、すべてのテスト結果とともに出力することで証明します。