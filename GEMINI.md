GEMINI.md — 完全自律で「Windows/Mac 両対応」アプリを完成させるための改善指示書
目的: 本プロジェクトの既存コードと仕様を読み取り、人手の操作（承認/質問/待機）を極力ゼロで、設計 → 実装 → テスト → バグ修正 → パッケージ化（Win: EXE/MSI, macOS: DMG/ZIP）まで自律的に完遂すること。

対象アプリ: 「アダアフィ用ショート動画支援アプリ（仮）」：

機能A: XのRT監視 → スクショ → 背景動画合成 → ショート動画自動生成/保存

機能B: TikTok/Instagram/YouTube のショート監視 → 転載向けフォーマットへ変換/保存

UI: ダッシュボード（監視状況と開始/設定ボタン）、設定（監視対象/間隔、動画出力：解像度・長さ・BGM・背景・上/下テロップ）

0. 自律実行ポリシー（最重要）
絶対に守るルール

起動は常に --yolo --checkpointing。破壊的変更の直前にスナップショット→ 失敗時は自動ロールバック（最新スナップショットを復元）して続行する。

確認ダイアログ禁止。不明点は合理的仮定で進め、ログに意思決定を記録する。

計画 → 実装 → テスト → 修正の短サイクルを自動反復。テスト失敗は自動修正して再実行。

連続失敗 3 回で計画を再立案。10 回でスコープ縮小（ユニット中心）→ それでも不可なら直前スナップショットへ復元。

危険コマンド（例：rm -rf /、レジストリ改変、グローバル npm -g、管理者権限の恒久変更）は禁止。代替手段で継続。

429/403/ネットワーク断は指数バックオフ（例：2s, 4s, 8s… 最大 5 分）＋上限リトライ。タイムアウト/回線断は待機モードへ移行し、回復後に再開。

ログはJSON Lines + 人間向けUIログの二系統。

すべてのチャット（エージェント応答・計画出力・エラーメッセージ）およびREADME/ログ説明は日本語で行う。コード/設定名などの固有名詞は英語可。

1. Definition of Done（完了基準）
Windows 10/11 と macOS（Intel/Apple Silicon）でビルド、パッケージ化、起動、動作がすべて成功する。

ダッシュボードから監視開始/停止が可能で、設定の変更がリアルタイム反映される。

新しいログイン方式（ブラウザ経由）がすべてのプラットフォームで正常に機能し、セッション情報が安全に保存・利用される。

機能A/B の監視→処理→保存が連続ジョブとして安定稼働する。

動画出力は指定のスキーマを満たし、ダミーデータを用いたテストで正常に出力されることを確認する。

ユニット/統合/E2Eの全テストがローカル & CIで成功する。

README に「動作要件/初回起動/未署名アプリの開き方（Gatekeeper回避）」が自動追記される。

2. 改善タスク一覧
2.1. Windows環境でのパッケージ化の成功【最優先】
現状の問題: Windows環境で npm run build によるビルドは成功するが、electron-builder を用いたパッケージ化（EXE/MSI生成）に失敗する。

指示:

electron-builder のログを詳細に解析し、エラーの原因を特定する。

考えられる原因（ネイティブモジュールの不整合、node-gyp の問題、asar のパッケージング設定、Windows特有のパス問題など）を体系的に調査し、解決策を実装する。

package.json の build セクションや、関連する設定ファイル（electron-builder.yml など）を修正し、Windowsでのパッケージ化を確実に成功させる。

成功後は、生成された .exe ファイルが正常にインストールおよび起動できることを確認する。

2.2. ログイン方式の刷新
現状の問題: ユーザー名とパスワードを直接入力する方式は、セキュリティリスクが高く、2FA（二要素認証）に対応できない。

指示:

現在のログインUI（ユーザー名/パスワード入力欄）を、「（プラットフォーム名）にログイン」ボタンに変更する。

このボタンをクリックすると、electron の session モジュールと playwright を連携させ、**外部ブラウザウィンドウ（または BrowserView）**を開き、各プラットフォームの公式ログインページを表示する。

ユーザーが手動でログインを完了し、プラットフォームのホームページなどにリダイレクトされたことを検知する。

ログイン成功後、その時点でのCookieまたはセッション情報を electron の session から安全に抽出し、keytar を使用してOSのセキュアストレージ（Keychain/Credential Manager）に保存する。

以降のスクレイピング処理では、保存されたCookie/セッション情報を利用して認証済み状態を維持する。

2.3. ファイル/フォルダ選択のUI改善
現状の問題: 出力先パス、BGMファイル、背景動画ファイルをテキスト入力で指定する必要があり、不便で入力ミスを招きやすい。

指示:

設定画面の各パス入力フィールドの横に「選択...」ボタンを設置する。

このボタンをクリックすると、ipcMain を介してMainプロセスを呼び出し、dialog.showOpenDialog APIを使用してOSネイティブのファイル/フォルダ選択ダイアログを表示する。

出力先フォルダの場合はフォルダ選択、BGM/背景動画の場合はファイル選択（適切な拡張子フィルタを設定）を許可する。

ユーザーが選択したパスを ipcRenderer を介してRendererプロセスに返し、設定画面の対応する入力フィールドに自動的にセットする。

3. テスト戦略の強化
3.1. 動画生成プロセスのテスト
目的: 実際のSNSデータに依存せず、動画生成ロジックが正しく機能することを確認する。

指示:

テスト用のダミーデータ（例: 1080x1920の背景動画ファイル、ダミーのスクリーンショット画像ファイル、短いBGMファイル）をプロジェクト内に用意する。

これらのダミーデータと固定の設定値（解像度、テロップ文言など）を用いて video-generator を実行するユニットテスト/統合テストを作成する。

生成された動画が、期待される解像度、長さ、フォーマットであり、テロップやBGMが正しく合成されているかを検証する。可能であれば、フレームハッシュを用いたゴールデン比較テストを導入する。

3.2. 新ログイン方式のテスト
目的: ブラウザ経由でのログインとセッション保存が、各プラットフォームで確実に機能することを確認する。

指示:

Playwright を用いたE2Eテストを作成する。

テスト内で「ログイン」ボタンをクリックし、表示されたブラウザでテスト用アカウントの認証情報を自動入力してログイン処理を実行する。

ログイン成功後、セッション情報が keytar によって正しく保存されたことを確認する。

保存されたセッション情報を用いて、再度スクレイピング処理（例: プロフィールページの読み込み）を実行し、認証済み状態でアクセスできることを検証する。

テストアカウント
X ID:rinnmamas2
X Password:pasowota427314t

3.3. 総合機能テスト
目的: 上記の改善とテストが完了した後、アプリケーション全体のワークフローが仕様通りに動作することを保証する。

指示:

テストシナリオを定義する（例: Xアカウントを設定 → ログイン → 監視開始 → RTを検知 → 動画が指定フォルダに正しく生成・保存される）。

このシナリオに基づき、手動テストまたはE2Eテストを実行し、初期設定から最終的な動画出力までの一連の流れに問題がないことを確認する。

すべての機能（機能A、機能B、設定変更の即時反映、ログ表示など）が、この指示書および元の仕様通りに実装されているか、網羅的にテストを実施する。

4. 実行シナリオ（エージェントの行動計画）
ワークスペース読取 → 既存コードとこの改善指示書を解析し、実行計画（箇条書き）を出力する。

環境点検 → Node/ffmpeg/ビルドツールを検出。無ければ自動導入する。

【最優先】パッケージ化修正 → 2.1. の指示に従い、Windowsでのパッケージ化問題を解決する。

ログイン機能改修 → 2.2. の指示に従い、ブラウザ経由のログイン方式を実装する。

UI改善 → 2.3. の指示に従い、ファイル/フォルダ選択ダイアログを実装する。

テスト実装と実行 → 3.1. と 3.2. の指示に従い、動画生成と新ログイン方式のテストを作成・実行する。失敗した場合は自動修正を試みる。

総合テスト → 3.3. の指示に従い、アプリケーション全体の動作を検証する。

ビルドとパッケージ化 → npm run build を実行し、Win/Mac 両方の成果物を生成する。

README 追記 → 環境要件、初回起動、未署名アプリの開き方、基本の使い方を追記する。

最終自己チェック → 1. Definition of Done を満たすか検証し、満たさない場合は不足分を実装して再テスト/再ビルドする。

（既存のセクション 0, 4-15 は、上記と矛盾しない限り、引き続き有効とします）

最後に
この指示書は「人手ゼロで走り切る」ための改善計画です。ここに記したルールと順序で、計画→実装→テスト→ビルドを 自律反復し、完了基準の達成をもって終了してください。
